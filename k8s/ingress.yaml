apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: k8s-devops-ingress
spec:
  rules:
  - host: k8s.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: k8s-devops-service
            port:
              number: 80